#header-region
.container
  #alerts-container
    - if notice
      = render 'layouts/alert', :alert => notice
  #main-region
    %h1= "Some data"
  #dialog-region

#footer-region
  = render 'layouts/footer'
= javascript_tag do
  $(function() {
  var current_user_object = {id: "#{ current_user.id}",email: "#{ current_user.email}"};
  NominaApp.Authorization.start({current_user_object: current_user_object});
  NominaApp.start();
  });
